schema_version = 1
family_id = "persistence_turbine_baseline"
display_name = "Persistence Turbine Baseline"
status = "archived"
summary = "Archived turbine-level persistence baseline on clean windows across all four datasets."
model_family = "persistence"
training_mode = "analytic_baseline"
implementation_root = "experiment/families/persistence"
readme_path = "experiment/families/persistence/README.md"
dataset_scope = ["kelmarsh", "penmanshiel", "hill_of_towie", "sdwpf_kddcup"]
supported_eval_protocols = ["full_series"]
supported_result_splits = ["full"]
supported_feature_protocols = ["power_only"]
implementation_label_kind = "entrypoint"
implementation_labels = ["persistence"]
notes = [
  "No repo-tracked experiment runner remains; reusable baseline logic lives in src/wind_datasets/persistence.py.",
]

[task_contract]
task_id = "next_6h_from_24h"
history_duration = "24h"
forecast_duration = "6h"
window_protocol = "dense_sliding"
split_protocol = "full_series_clean_windows"
granularity = "turbine"

[implementation_bindings]
persistence = "power_only"
