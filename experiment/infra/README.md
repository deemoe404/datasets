# Experiment Infra

This directory contains shared experiment infrastructure rather than
model-specific code.

- `common/`: reusable helpers for feature protocols, window protocols, registry loading, run records, and published output paths
- `registry/`: machine-readable experiment family and feature-protocol registry
