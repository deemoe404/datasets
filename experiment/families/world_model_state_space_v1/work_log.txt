2026-04-16 
- 当天先有一次 formal rerun（`20260416-070708` manifest，结果文件已被后续 rerun 覆盖），随后在 `raw_wake_geometry_fix` 之后保留了当前 canonical publish（`20260416-144026-raw_wake_geometry_fix`）。
- 当前保留的 canonical `world_model_state_space_v1_farm_sync`（`batch_size=432`, `seed=3407`）指标：val rolling RMSE/MAE = `0.164508 / 0.106924`，val non_overlap RMSE/MAE = `0.162985 / 0.105974`，test rolling RMSE/MAE = `0.172678 / 0.112051`，test non_overlap RMSE/MAE = `0.172004 / 0.111609`，best epoch = `6 / 16`。
- 当天还做了 `kelmarsh_ablation_smoke.csv` 的 1 epoch CPU smoke，覆盖 canonical / `wake_off` / `graph_off` / `no_farm_aux` / `no_met_aux`。这个 smoke 只说明链路和 variant 选择逻辑是通的，不适合用于 ablation 排名；例如 smoke 下 `wake_off` 一度优于 canonical，但后续正式预算并不成立。
- 结论：raw wake geometry 修复后的 canonical 已经能稳定完整跑通；正式预算下 non-overlap 指标略优于 rolling。

2026-04-17
- 跑了 `kelmarsh_ablation_batch.csv`，正式预算比较 canonical、`wake_off`、`graph_off`、`no_farm_aux`、`no_met_aux`。
- 相对 canonical 的 test non_overlap RMSE `0.172004`：`no_farm_aux -> 0.169290`（提升 `0.002714`，当前保留 batch ablation 最强），`no_met_aux -> 0.171696`（提升 `0.000309`，但 val rolling 从 `0.164508` 变成 `0.164977`，收益不稳定），`wake_off -> 0.173176`（退化 `0.001172`），`graph_off -> 0.177885`（退化 `0.005881`）。
- 这批结果说明：图聚合比当前 wake 分支更关键，去掉图结构会明显伤害泛化；动态 wake 特征有帮助，但贡献幅度小于图聚合；`farm_aux` 在默认 `0.1` 权重下更像副作用而不是收益项。
- 同日跑了 `search_20260417` 的 `farm_loss_weight` sweep。final val rolling RMSE：`0.05 -> 0.162381`，`0.00 -> 0.162394`，`0.10 -> 0.164508`；final test non_overlap RMSE：`0.00 -> 0.169290`，`0.05 -> 0.169803`。`selected_defaults.json` 依验证集规则选了 `0.05`，但测试集实际是 `0.00` 更好。
- 结论：`farm_loss_weight=0.1` 偏大，farm auxiliary loss 至少应降到 `0.05`，甚至可以直接关掉；当前 `no_farm_aux` 比默认 canonical 更像下一轮默认候选。

2026-04-18
- 代码层新增 `residual_persistence` head（commit `5bf97f4`），意图验证“预测 residual over last-value persistence anchor”是否比直接预测更稳。
- 横向对照截至本地 2026-04-18 published artifacts：`world_model_rollout_v1` test non_overlap RMSE = `0.169162`，`world_model_agcrn_v1` test non_overlap RMSE = `0.170313`，best baseline test non_overlap RMSE = `0.171152`（`world_model_dgcrn_v1_farm_sync`）。
- 结论：state-space 主线即便只看 `no_farm_aux` batch ablation（test non_overlap RMSE `0.169290`）也已经优于现有 baseline family 和 AGCRN world-model；与 rollout baseline 基本同一量级。

2026-04-19
- 跑了 `residual_vs_canonical_cuda4090.csv`，在同一设置（`batch_size=256`）下对比 canonical 与 `world_model_state_space_v1_residual_persistence_farm_sync`。
- canonical：val/test non_overlap RMSE = `0.162490 / 0.169531`，best epoch = `5`；residual_persistence：val/test non_overlap RMSE = `0.162138 / 0.168685`，best epoch = `3`。
- 相对 canonical，residual head 在 test non_overlap 上再提升 `0.000846`，在 test rolling 上提升 `0.000659`，MAE 也同步下降；但 val rolling RMSE 略差（`0.164162` vs `0.163861`）。
- 结论：residual head 看起来对测试泛化有正收益，而且收敛更早，但验证集收益不够一致，暂时还不适合直接替换默认头；需要做额外复现实验。当前 `residual_head_plan_20260419/queue_status.txt` 仍显示有一条复现实验在跑，先不把它记成已确认结论。

2026-04-21
- `residual_head_plan_20260419_farm0p05` 队列已于 `2026-04-21T06:27:48+08:00` 完成；scratch root: `experiment/artifacts/scratch/world_model_state_space_v1/residual_head_plan_20260419_farm0p05`。
- `farm_loss_weight=0.05`、`val_rmse_pu` 复现：canonical val/test rolling RMSE = `0.165483 / 0.173988`，MAE = `0.109451 / 0.114931`，best epoch = `5 / 15`；`residual_persistence` val/test rolling RMSE = `0.162973 / 0.169035`，MAE = `0.106281 / 0.109889`，best epoch = `3 / 13`。这次 residual 不只是测试集占优，验证集 overall rolling RMSE/MAE 也更好。
- carry-over scratch eval 没有支持“在线状态延续有额外收益”的故事：canonical val/test overall RMSE 从 `0.165480 / 0.173977` 变成 `0.166228 / 0.175148`，residual 从 `0.163004 / 0.169064` 变成 `0.163559 / 0.169569`，两边都是 `rolling_origin_carry_over` 略差于 `rolling_origin_no_refit`。
- `farm_loss_weight=0.0` 的双 contract 最小复跑中，absolute 在 `val_rmse_pu` / `val_mae_pu` 选择下的 val RMSE 分别为 `0.162922 / 0.164449`，test rolling RMSE 为 `0.169621 / 0.175486`；residual 对应 val RMSE 为 `0.162248 / 0.162402`，test rolling RMSE 为 `0.168571 / 0.168628`。也就是说 residual 在两种 selection metric 下都压过 absolute。
- 当前结论：`residual_persistence` 已经足够升格为主线 family 的 experimental working head，但还不该直接翻成 official default；更合理的故事是“decoder-side 参数化改进已经被确认”，而不是“carry-over / online state persistence 已经成立”。

2026-04-22
- derived-ramp auxiliary 从单尺度 `K=1` 升级成 `ramp v2`：固定多尺度一阶差分 `K={3,6}`。当前 family 仍只暴露 `ramp_loss_weight` / `ramp_huber_delta` 两个公共 knob；scale 不对外开放成 CLI。
- 新的 ramp 监督统一按“prepend 最后 `K` 个真实 history target，再做 `K-step diff`”实现：`h<=K` 时回看 history 尾部真实值，`h>K` 时回看 `forecast[t-K]`。mask 仍坚持双端真实有效，禁止把 persistence fallback anchor 当作 ramp supervision 目标。
- 训练侧把 ramp loss 改成多尺度等权平均：`L_ramp = mean(L_k)` for `K in {3,6}`，同时把 `train_ramp_loss_k3_*` / `train_ramp_loss_k6_*` 接进 training history 和 TensorBoard；formal result rows 增加 `ramp_scale_steps="3,6"` 追踪字段。
- scratch eval 侧新增 `ramp_mae_pu_k3` / `ramp_rmse_pu_k3` / `sign_agreement_rate_k3` 以及对应 `k6` 列；原有 aggregate ramp 指标现在表示 active scales 的聚合结果。
- 性能修复先落在 checkpoint scratch eval：`rolling_origin_no_refit` 改成 batched path，不再逐 window 调 `filter_history`；`rolling_origin_carry_over` 仍保持顺序 state persistence 语义，但和 no-refit 共用同一套 metrics accumulator，减少重复逻辑和 host/device 往返。
- 代码层新增两个 decoder-side 结构变体：`global_local_residual` 与 `global_local_increment`。两者都把 `global` 固定为 whole-farm shared component，并对 local branch 做 weighted zero-mean 约束；第一版不引入 cluster state，也不学习额外的 `alpha_i` mixing。
- `global_local_residual` 直接在 persistence anchor 上加“全场共享 residual + 去均值 local correction”；`global_local_increment` 则把“全场共享 increment + 去均值 local increment correction”从同一个 persistence anchor 逐步累积回 level。当前这一轮只完成代码、测试和文档同步，实验仍待后续统一排期。
- 当前这轮改动没有触碰训练主干 `_aggregate()` / `144+36` 的结构性热点，也没有把 ramp 接到 `persistent_chunk6`；`chunk6 v1` 仍应保持 `ramp_loss_weight=0.0`。
- 旧版单尺度 ramp 队列 `ramp_ablation_20260421_residual_farm0p00_val_rmse` 到此为止正式封存：`ramp0p00` train + scratch eval 已完成，`ramp0p02` train 已完成但 carry-over scratch eval 在 CPU-bound 顺序路径上运行超过 8 小时仍未产出 summary/diagnostics，因此按主线判断价值不足而主动放弃；`ramp0p05` 在启动前取消。
- 这次旧队列留下的主线结论是：单尺度 `K=1` ramp 在当前 residual/no-farm trunk 上没有给出足够干净的收益信号，而且旧 checkpoint eval 路径的 wall-clock 成本过高；后续 ramp 实验应直接基于 `ramp v2` 和 batched no-refit eval 重开，不再继续追加这条旧队列。
- 本地代码层已补上 residual 路径的 edge-gating 2x2 实现面：以 `world_model_state_space_v1_residual_persistence_farm_sync` 为控制，再新增 `residual_persistence_gated_sum`、`residual_persistence_rotor_units_wake`、`residual_persistence_gated_sum_rotor_units_wake` 三个显式 variants。第一轮仍不把 `aggregation_kind` / `wake_units_kind` 暴露成 family-wide CLI 组合轴。
- `_wake()` 现在支持两种内部解释：旧的 `legacy_mixed_units` 继续直接用 raw `dx_m/dy_m` 除以 `distance_in_rotor_diameters`，新的 `rotor_diameter_units` 先按 `meters_per_rotor_diameter = hypot(dx,dy) / distance_in_rotor_diameters` 把 `dx,dy` 转成叶轮直径单位，再算 `d_parallel` / `d_cross`。`_aggregate()` 也新增了 residual-only 的 `gated_sum` 分支，用来和当前 `normalized_gated_average` 做干净对照。
- checkpoint scratch eval 现在可选 `--include-edge-diagnostics`，会在原有 `*.csv` / `*.diagnostics.csv` / `*.summary.csv` 旁边额外写 `*.edge_diagnostics.csv`。这份输出只做 phase-wise 聚合诊断，不改默认训练路径，也不改 formal publish schema。当前这轮只完成本地代码、测试和文档同步，实验继续等后续有空余算力再排。

2026-04-23
- Ubuntu `global_local_ablation_20260422_farm0p00_val_rmse` 队列已完成；scratch root: `experiment/artifacts/scratch/world_model_state_space_v1/global_local_ablation_20260422_farm0p00_val_rmse`。固定设置为 `farm_loss_weight=0.0`、`ramp_loss_weight=0.0`、`selection_metric=val_rmse_pu`、`batch_size=432`，两条线都只做 train + no-refit eval。
- `global_local_residual`：val/test rolling RMSE = `0.161666 / 0.169643`，MAE = `0.106090 / 0.111140`，test non_overlap RMSE = `0.168121`；no-refit test overall `ramp_rmse_pu = 0.121335`，`sign_agreement_rate = 0.537487`，`k3 = 0.110702`，`k6 = 0.131112`。
- `global_local_increment`：val/test rolling RMSE = `0.162425 / 0.178203`，MAE = `0.107249 / 0.116558`，test non_overlap RMSE = `0.176716`；no-refit test overall `ramp_rmse_pu = 0.122373`，`sign_agreement_rate = 0.523707`，`k3 = 0.111559`，`k6 = 0.132311`。
- 相对当前 residual trunk 最优的 `ramp0p05`（test rolling/non_overlap RMSE = `0.168230 / 0.166406`），两条 global/local decoder 变体都没有给出正收益；其中 `global_local_increment` 明显更差，说明“从 persistence anchor 逐步累积 global/local increment”这条参数化在现有 trunk 上不稳。
- 当前结论：`global_local_residual` 仍可保留为一个 clean decoder decomposition 备选，但不应升格为新的 working head；主线 working head 仍是 `residual_persistence`，而 `global_local_increment` 暂不值得继续追加预算。

- Ubuntu residual edge-gating `2x2` 队列已完成；scratch root: `experiment/artifacts/scratch/world_model_state_space_v1/edge_gating_2x2_20260423_residual_farm0p00_val_rmse`。固定设置为 `farm_loss_weight=0.0`、`ramp_loss_weight=0.0`、`selection_metric=val_rmse_pu`、`batch_size=432`，4 个单元都完成 train + no-refit eval + edge diagnostics。
- `residual_baseline`：val/test rolling RMSE = `0.162090 / 0.169124`，test non_overlap RMSE = `0.167406`，test ramp RMSE = `0.121198`，sign agreement = `0.537017`。
- `gated_sum`：val/test rolling RMSE = `0.161424 / 0.169829`，test non_overlap RMSE = `0.168675`，test ramp RMSE = `0.121238`，sign agreement = `0.536705`。验证集更好但测试集退化；单独改 `gated_sum` 不支持继续升格。
- `rotor_units_wake`：val/test rolling RMSE = `0.162393 / 0.170105`，test non_overlap RMSE = `0.169225`，test ramp RMSE = `0.121367`，sign agreement = `0.537090`。wake 单位修正后 edge diagnostics 变正常，test transition/update `wake_gate_mean` 从 baseline 的约 `0.0005 / 0.0009` 提到 `0.397 / 0.409`，但主指标退化。
- `gated_sum_rotor_units_wake`：val/test rolling RMSE = `0.161994 / 0.168980`，test non_overlap RMSE = `0.167588`，test ramp RMSE = `0.121244`，sign agreement = `0.537432`。这是本组唯一 test rolling 略优于 baseline 的新增单元，但 non_overlap 和 ramp 仍略差于 baseline，幅度不足以推翻当前 working head。
- 当前结论：这组 residual edge-gating `2x2` 没有产生稳定优于 `residual_persistence` baseline 的新结构。`rotor_diameter_units` 修复了 wake-gate 饱和的语义问题，但单独或与 `gated_sum` 组合都没有给出足够干净的泛化收益；主线 working head 仍保持 `residual_persistence`。

2026-04-24
- Phase-1 hardened adapter baselines completed on Ubuntu as auxiliary baseline evidence for the main `world_model_state_space_v1` track; family: `world_model_hardened_baselines_v1`; artifacts were renamed to `experiment/artifacts/published/world_model_hardened_baselines_v1/20260424-phase1-adapter-*.csv` plus per-backend `training_history.csv`. Important claim boundary: these rows record official source provenance, but DGCRN/TimeXer/iTransformer execution still delegated to repo-local `world_model_baselines_v1` backends. They are adapter sanity checks, not final official implementation baselines.
- Chronos-2 follow-up: the earlier tuned-full failure was `exit 139` during the second zero-shot selection-metric pass. The runner now reuses rows/history for identical zero-shot Chronos selection metrics; resume run finished successfully and produced the phase-1 Chronos-2 CSV and history file.
- Test `non_overlap` phase-1 adapter ranking by RMSE/MAE: DGCRN annotated official-core `0.173588 / 0.121311`; iTransformer annotated official `0.179042 / 0.117165`; Chronos-2 zero-shot official `0.181359 / 0.116055`; last-value persistence hardened `0.184142 / 0.117510`; TimeXer annotated official `0.208640 / 0.150263` under `val_rmse_pu` selection and `0.208811 / 0.150219` under `val_mae_pu` selection.
- Test `rolling_origin_no_refit` phase-1 adapter ranking by RMSE/MAE: DGCRN annotated official-core `0.174092 / 0.121987`; iTransformer annotated official `0.179679 / 0.117417`; Chronos-2 zero-shot official `0.183834 / 0.117345`; last-value persistence hardened `0.186432 / 0.119180`; TimeXer annotated official `0.209018 / 0.150712` under `val_rmse_pu` selection and `0.209218 / 0.150685` under `val_mae_pu` selection.
- Interpretation: the phase-1 adapter surface is tougher than the older repo-local/style baseline surface, but it is not sufficient for final thesis claims. `world_model_official_baselines_v2` is now the paper-grade hardening target; proposed state-space architecture work is frozen until v2 gates/search/multi-seed evidence is available.
- No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`: this was an auxiliary baseline result recording step, not a new mainline state-space module or output head.

2026-04-25
- Synced current `world_model_official_baselines_v2` execution state from `experiment/families/world_model_official_baselines_v2/.work/completion_queue_20260425.md` and published/scratch artifacts. The v2 comparison surface is not fully closed yet; only DGCRN currently has five-seed paper-table-adjacent evidence.
- DGCRN official-core residual B2 completed the 50-trial validation search (`experiment/artifacts/scratch/world_model_official_baselines_v2/dgcrn_residual_formal_search_20260424/formal_search_summary_50.csv`), frozen top3 test-once (`experiment/artifacts/published/world_model_official_baselines_v2/20260425-dgcrn-top3-test-once.csv`), and trial20 five-seed summary (`experiment/artifacts/published/world_model_official_baselines_v2/20260425-dgcrn-trial20-multiseed-summary.csv`). Trial20 five-seed test RMSE/MAE mean: non_overlap `0.227296 / 0.173005`, rolling no-refit `0.227799 / 0.172652`; selected by validation only, test once per seed.
- DGCRN bootstrap/significance is explicitly blocked, not negative: `experiment/artifacts/published/world_model_official_baselines_v2/20260425-dgcrn-trial20-multiseed-bootstrap-status.json` records `missing_per_origin_paired_errors`; aggregate seed rows must not be resampled as paired origin evidence.
- TimeXer official residual B0/B2 has validation-only selection artifacts but no test-once result yet. Target-only residual trial09 passed separate overfit64 preflight and had val RMSE `0.214727` non_overlap / `0.224919` rolling; full-exog residual trial02 also passed gates but was worse on validation (`0.216344` / `0.226456`), so the queue advanced rather than spending more TimeXer budget.
- iTransformer official residual has validation selection plus one test-once pair. Direct target-only rows had lower validation RMSE but failed Gate C continuity and are diagnostic only. The continuity-safe residual choices were weak: trial16 target-only B0 test RMSE `0.266495` non_overlap / `0.267040` rolling, trial24 target-plus-exog B2 test RMSE `0.263650` non_overlap / `0.263947` rolling.
- TFT-PF per-turbine remains blocked for paper-grade full rolling: `experiment/families/world_model_official_baselines_v2/README.md` records that full rolling test-once destabilized the Ubuntu CUDA host; smoke/overfit64/validation/bounded-test diagnostics are adapter evidence only until a redesigned streaming evaluator passes the bounded-to-full recovery ladder.
- Chronos-2 v2 has only bounded validation smoke, not a formal result: `chronos2_val_nonoverlap_smoke_20260425.csv` and `chronos2_val_rolling128_20260425.csv` covered limited validation origins with RMSE `0.243310` non_overlap smoke and `0.231023` rolling-128 smoke. Do not treat bounded smoke as paper-grade validation/test evidence.
- MTGNN official-core calendar residual B1 failed Gate B in the 64-window preflights: `mtgnn_calendar_residual_overfit64_20260425.csv` RMSE/MAE `0.645773 / 0.528365`, retry `mtgnn_calendar_residual_overfit64_retry_20260425.csv` RMSE/MAE `0.497939 / 0.415511`, both `train_gate_after_fit_passed=false`. Do not launch formal MTGNN search until Gate B recovery is close.
- Claim boundary for mainline: v2 is still an active baseline hardening queue, not a complete final baseline table. Current state-space paper comparisons should mention DGCRN five-seed evidence separately from incomplete TimeXer/iTransformer/TFT/Chronos/MTGNN coverage. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`, because this sync adds no new state-space module or output head.

2026-04-25 addendum
- Later v2 queue sync supersedes the earlier same-day partial notes. DGCRN trial20 now has generated five-seed statistics summary (`experiment/artifacts/published/world_model_official_baselines_v2/20260425-dgcrn-trial20-multiseed-statistics-summary.csv`); bootstrap/significance remains fail-closed via `20260425-dgcrn-trial20-multiseed-bootstrap-status.{csv,json}` because paired per-origin errors are missing.
- TimeXer target-only residual trial09 completed validation-frozen test-once (`experiment/artifacts/published/world_model_official_baselines_v2/20260425-timexer-trial09-test-once.csv`; manifest `experiment/artifacts/runs/world_model_official_baselines_v2/20260425-054756-timexer_trial09_test_once_20260425/manifest.json`). Test RMSE/MAE: rolling `0.272861 / 0.197942`, non_overlap `0.244819 / 0.182359`; config was selected by validation only with `no_test_feedback=true`.
- MTGNN calendar residual B1 independent Gate B preflight passed only under train-selected overfit64 diagnostics (`mtgnn_calendar_residual_b1_gate_b_overfit64_preflight_status_20260425.json`, RMSE/MAE `0.027522 / 0.014203`). The validation-selected checkpoint remains weak (`mtgnn_calendar_residual_b1_validation_selected_preflight_gateb_20260425.csv`, rolling val RMSE/MAE `0.251076 / 0.193751`, non_overlap val `0.248516 / 0.190739`), so MTGNN must not advance to formal search or test-once.
- Chronos-2 has paper-grade full non_overlap validation/test evidence from `20260424-formal-tuning-chronos2-nonoverlap.csv` (val RMSE/MAE `0.174911 / 0.113817`, test `0.181359 / 0.116055`) and the future-target leakage check passed. Full rolling remains blocked; the 128/512/2048 bounded rolling ladder is not paper-grade and should not be reported as a full rolling row.
- TFT-PF streaming evaluator was added and bounded legacy-vs-streaming comparisons passed for direct/residual 128-origin validation. The streaming 512/2048 validation ladder completed, but the full rolling validation attempt crashed with native `*** stack smashing detected ***`; no full validation/test manifest or output CSV exists, so there is no paper-grade TFT row.
- No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`: these were baseline/status artifacts only, with no new mainline state-space module or output head.

2026-04-25 DGCRN bootstrap follow-up
- Added v2 formal runner per-origin error export via `--origin-error-output-path`, with direct `baseline_abs_error_pu` / `proposed_abs_error_pu` columns for last-value persistence vs the evaluated official baseline row.
- Reran DGCRN trial20 five seeds sequentially, not in parallel, at `experiment/artifacts/scratch/world_model_official_baselines_v2/dgcrn_trial20_multiseed_origin_errors_20260425/`; published combined seed rows and per-origin comparison at `20260425-dgcrn-trial20-multiseed-origin-errors-seed-rows.csv` and `20260425-dgcrn-trial20-multiseed-origin-errors-comparison.csv`.
- Protocol-specific bootstrap is now completed rather than blocked. Rolling uses `5120` seed-origin rows; non_overlap uses `145`. The statistics summaries reproduce the earlier DGCRN trial20 five-seed metric means: rolling RMSE/MAE `0.227799 / 0.172652`, non_overlap `0.227296 / 0.173005`.
- Bootstrap direction is `baseline_abs_error_pu - proposed_abs_error_pu`, so negative values mean DGCRN has larger origin-level AE than last-value persistence. Rolling paired delta mean/95% CI = `-0.035244 [-0.036881, -0.033696]`, block delta = `-0.035286 [-0.042134, -0.028737]`, `prob_delta_gt_zero=0.0`; non_overlap paired delta = `-0.038638 [-0.047410, -0.029884]`, block delta = `-0.038954 [-0.049120, -0.028711]`, `prob_delta_gt_zero=0.0`.
- Claim boundary: DGCRN trial20 is no longer missing bootstrap evidence for the persistence comparison, but the statistical conclusion is unfavorable to DGCRN. This does not provide a positive baseline challenge to the current state-space mainline. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 TimeXer multiseed follow-up
- Reran the validation-frozen TimeXer target-only residual trial09 config for seeds `3407,42,2026,17,926` with per-origin export at `experiment/artifacts/scratch/world_model_official_baselines_v2/timexer_trial09_multiseed_origin_errors_20260425/`.
- Published combined seed rows and per-origin comparison at `20260425-timexer-trial09-multiseed-origin-errors-seed-rows.csv` and `20260425-timexer-trial09-multiseed-origin-errors-comparison.csv`; protocol-specific summaries/bootstrap live under `20260425-timexer-trial09-multiseed-origin-errors-{rolling,nonoverlap}-*`.
- Five-seed test RMSE/MAE mean: rolling `0.281506 / 0.203844`, non_overlap `0.256614 / 0.190883`. This is weaker than the single-seed trial09 row and clearly not competitive with persistence or the current state-space track.
- Persistence-comparison bootstrap completed and is also unfavorable to TimeXer. Rolling paired delta = `-0.066408 [-0.069582, -0.063300]`, block delta = `-0.066326 [-0.079607, -0.053698]`, `prob_delta_gt_zero=0.0`; non_overlap paired delta = `-0.056252 [-0.073056, -0.040029]`, block delta = `-0.055758 [-0.077607, -0.035803]`, `prob_delta_gt_zero=0.0`.
- Claim boundary: TimeXer trial09 now has five-seed mean/std and completed bootstrap status for the persistence comparison, but it should be reported as a weak official baseline, not as a challenger. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 TFT native-crash triage
- Ran validation-only TFT-PF residual B2 trial02 crash triage under `experiment/artifacts/scratch/world_model_official_baselines_v2/tft_streaming_eval_20260425/`; no test split was evaluated.
- With frozen config (`hidden_size=32`, `lstm_layers=1`, `attention_head_size=4`, `hidden_continuous_size=16`, `dropout=0.1`, `learning_rate=0.0003`, `residual_anchor_steps=1`, `max_train_origins=512`, `max_checkpoint_origins=256`, chunk size `512`), validation rolling rungs `4096`, `8192`, and `12288` completed and wrote CSV/summary/manifest. Last passing rung `12288`: window_count `12288`, prediction_count `2636136`, RMSE/MAE `0.180933 / 0.122437`.
- First failing rung `16384` exited `134` after training with native `*** stack smashing detected ***: terminated`; no CSV/summary/manifest was written. The full validation size remains `47139` windows and was not rerun after the failure.
- Triage artifact: `experiment/artifacts/scratch/world_model_official_baselines_v2/tft_streaming_eval_20260425/residual_val_rolling_native_crash_triage_20260425.json`.
- Claim boundary: TFT remains blocked for full validation and test-once. The next engineering step is a subprocess-per-shard or model-reload evaluation design, then rerun `12288 -> 16384 -> full` validation before any test row. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 Chronos rolling-shard recovery
- Added a recoverable Chronos-2 rolling shard evaluator at `experiment/families/world_model_official_baselines_v2/diagnostics/chronos2_rolling_shards.py`, with `plan`, `eval-shard`, and `aggregate` commands. The CLI defaults to validation only, requires `--allow-test` for test split, records `uses_future_target=false`, writes exact AE arrays, and fail-closes on coverage gaps or exact-quantile overflow.
- Validation rolling plan: `experiment/artifacts/scratch/world_model_official_baselines_v2/chronos2_val_rolling_shards_20260425/chronos2_kelmarsh_val_rolling_origin_no_refit_plan.json`; total validation rolling windows `47139`, forecast steps `36`, nodes `6`, max prediction count `10182024`, exact AE limit `12000000`.
- Full validation rolling shard run completed and aggregated at `experiment/artifacts/scratch/world_model_official_baselines_v2/chronos2_val_rolling_shards_20260425/chronos2_kelmarsh_val_rolling_origin_no_refit_aggregate.{json,csv}`. Aggregate metrics: RMSE/MAE `0.177025 / 0.114238`, short/mid/long RMSE `0.110636 / 0.160925 / 0.203300`, lead1 RMSE/MAE `0.073777 / 0.044858`, AE p50/p90/p95 `0.066070 / 0.295827 / 0.399516`, exact quantile status `exact`.
- One 1024-origin shard (`40960:41984`) terminated once with signal 7 before writing artifacts. It was recovered by two 512-origin shards (`40960:41472` and `41472:41984`), after which aggregate coverage closed over all `47139` validation origins. The final aggregate used `48` source shard records because of this split.
- Claim boundary: Chronos-2 now has paper-grade full validation rolling evidence and earlier full non-overlap validation/test evidence, but no rolling test-once row was launched in this step. Keep test split frozen until the validation result is reviewed and an explicit test-once run is authorized. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 TFT-PF recovery-shard validation
- Added a recoverable TFT-PF rolling shard evaluator at `experiment/families/world_model_official_baselines_v2/diagnostics/tft_pf_rolling_shards.py`, with validation-only `plan`, `eval-shard`, and `aggregate` commands; `test` requires `--allow-test`. The CLI defaults to the residual B2 trial02-style frozen config (`hidden_size=32`, `lstm_layers=1`, `attention_head_size=4`, `hidden_continuous_size=16`, `dropout=0.1`, `learning_rate=0.0003`, `residual_anchor_steps=1`, `max_train_origins=512`, `max_checkpoint_origins=256`, `max_epochs=8`, chunk size `512`).
- The recovery design trains the same frozen TFT config inside each shard process, evaluates only that shard, and writes JSON/CSV metrics, exact absolute-error `.npy`, and per-origin proposed AE CSVs. Aggregation fail-closes on missing/overlapping coverage, split/protocol/variant/config mismatch, shape/rated-power mismatch, non-exact shard quantiles, aggregate exact-AE overflow, or missing `uses_future_target=false`.
- Validation recovery ladder completed without using test split. Rung `12288` aggregated at `experiment/artifacts/scratch/world_model_official_baselines_v2/tft_pf_val_rolling_shards_12288_20260425/tft_pf_residual_kelmarsh_val_rolling_origin_no_refit_aggregate.{json,csv}` with RMSE/MAE `0.180933 / 0.122437`, matching the earlier single-process last-pass metrics. Rung `16384` aggregated at `experiment/artifacts/scratch/world_model_official_baselines_v2/tft_pf_val_rolling_shards_16384_20260425/tft_pf_residual_kelmarsh_val_rolling_origin_no_refit_aggregate.{json,csv}` with RMSE/MAE `0.177418 / 0.119873`; initial shard 0 hit signal 11 and shard 2 hit signal 6/stack-smashing, but both recovered on one 4096-origin retry.
- Full validation rolling completed at `experiment/artifacts/scratch/world_model_official_baselines_v2/tft_pf_val_rolling_shards_full_20260425/tft_pf_residual_kelmarsh_val_rolling_origin_no_refit_aggregate.{json,csv}`. It covers all `47139` validation rolling origins and `10139796` valid predictions with `12` source shard records, exact AE status `exact`, and `uses_future_target=false`. Aggregate metrics: RMSE/MAE `0.213705 / 0.147869`, short/mid/long RMSE `0.146822 / 0.199651 / 0.239959`, lead1 RMSE/MAE `0.074311 / 0.044390`, AE p50/p90/p95 `0.097460 / 0.353345 / 0.466045`.
- Full validation still exposed native instability: full shard 10 (`40960:45056`) hit signal 6/stack-smashing once, then succeeded on one retry. This is now a recoverable shard-level failure, not a full-run blocker, but it should be reported as runtime fragility. TFT-PF now has full validation rolling evidence but still no full rolling test-once row; do not run test until the validation result is reviewed and an explicit test-once is authorized. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 Chronos rolling test-once
- After explicit approval to proceed Chronos first, ran the frozen Chronos-2 zero-shot B2 full rolling `test` evaluation with the recoverable shard CLI under `experiment/artifacts/scratch/world_model_official_baselines_v2/chronos2_test_rolling_shards_20260425/`.
- Test plan covered `94458` rolling origins, `36` forecast steps, `6` nodes, and `20300940` valid predictions with exact AE limit `24000000`. The main 1024-origin loop wrote `89` shards; four shards (`46080:47104`, `56320:57344`, `71680:72704`, `93184:94208`) terminated once with native signal 11 and were recovered by whole-shard retries.
- Final aggregate: `experiment/artifacts/scratch/world_model_official_baselines_v2/chronos2_test_rolling_shards_20260425/chronos2_kelmarsh_test_rolling_origin_no_refit_aggregate.{json,csv}`. It records `selected_by=validation_only`, `no_test_feedback=true`, `uses_future_target=false`, `ae_quantile_status=exact`, and continuous coverage across all `93` source shards.
- Chronos-2 rolling test metrics: RMSE/MAE `0.183834 / 0.117345`, short/mid/long RMSE `0.111655 / 0.164518 / 0.213050`, lead1 RMSE/MAE `0.074979 / 0.045681`, AE p50/p90/p95 `0.067056 / 0.303207 / 0.415298`.
- Claim boundary: this closes Chronos-2 rolling Gate E test-once for the frozen zero-shot config. It is still a single zero-shot configuration, not a multiseed trainable baseline. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 TFT-PF rolling test-once
- After explicit approval to proceed TFT after Chronos, ran the frozen TFT-PF residual B2 trial02-style config on full rolling `test` with the recoverable shard CLI under `experiment/artifacts/scratch/world_model_official_baselines_v2/tft_pf_test_rolling_shards_full_20260425/`.
- The first 4096-origin attempts failed before artifact write, so recovery switched to a conservative script at `experiment/families/world_model_official_baselines_v2/.work/tft_test_recovery_20260425.sh`: it ran one TFT subprocess at a time, used 2048-origin primary segments, retried failed segments, and split persistently failing segments to 1024 origins. The run completed continuous coverage over all `94458` rolling test origins and `20300940` valid predictions, then aggregated automatically.
- Final aggregate: `experiment/artifacts/scratch/world_model_official_baselines_v2/tft_pf_test_rolling_shards_full_20260425/tft_pf_residual_kelmarsh_test_rolling_origin_no_refit_aggregate.{json,csv}`. It records `selection_metric=val_overall_rmse`, `selected_by=validation_only`, `no_test_feedback=true`, `uses_future_target=false`, and `ae_quantile_status=exact`.
- Test aggregate used `49` source shards: `44` shards of 2048 origins, `4` shards of 1024 origins, and one 250-origin tail. Native fragility remained visible but recoverable: failed attempts occurred at `16384:18432` signal 11, `22528:24576` signal 6/stack-smashing, `38912:40960` signal 7, `40960:43008` two signal-6 failures before 1024 split, `61440:63488` signal 6, and `86016:88064` signal 6.
- TFT-PF rolling test metrics: RMSE/MAE `0.220489 / 0.152684`, short/mid/long RMSE `0.149128 / 0.203445 / 0.249448`, lead1 RMSE/MAE `0.075452 / 0.045377`, AE p50/p90/p95 `0.101229 / 0.361670 / 0.481535`.
- Claim boundary: this closes TFT-PF full rolling Gate E test-once for the frozen validation-selected config, but it remains a weak and runtime-fragile official baseline. It is not yet a multiseed/bootstrap-complete paper-table row. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 TFT-PF rolling bootstrap follow-up
- Added a TFT shard-origin error enrichment diagnostic at `experiment/families/world_model_official_baselines_v2/diagnostics/enrich_tft_origin_errors.py`. It reads the completed TFT rolling aggregate, verifies each shard origin-error CSV against the prepared test rolling windows, computes last-value persistence per-origin AE with the existing formal helpers, and writes paired comparison rows.
- Published TFT-PF rolling paired-origin comparison and bootstrap/status artifacts at `experiment/artifacts/published/world_model_official_baselines_v2/20260425-tft-pf-trial02-rolling-test-origin-errors-comparison.csv` and `20260425-tft-pf-trial02-rolling-test-origin-errors-bootstrap-status.{json,csv}`.
- The comparison covers all `94458` rolling test origins. Origin-level mean AE is persistence/TFT `0.119039 / 0.152564`; the aggregate prediction-weighted TFT MAE remains `0.152684`.
- Bootstrap direction is `baseline_abs_error_pu - proposed_abs_error_pu`, so negative values mean TFT-PF has larger origin-level AE than last-value persistence. Rolling paired delta mean/95% CI = `-0.033521 [-0.033901, -0.033139]`, block delta = `-0.033511 [-0.034976, -0.032080]`, `prob_delta_gt_zero=0.0`.
- Claim boundary: TFT-PF now has single-seed rolling bootstrap/status evidence for the persistence comparison, but it still lacks multiseed mean/std. This is a weak/runtime-fragile official baseline, not a positive challenger to the current state-space mainline. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 Chronos rolling bootstrap follow-up
- Added a Chronos shard-origin enrichment diagnostic at `experiment/families/world_model_official_baselines_v2/diagnostics/enrich_chronos_origin_errors.py`. Chronos shards did not store `origin_error_path` CSVs, so this script reconstructs per-origin proposed AE from each shard `.abs_errors.npy` using prepared-window valid-count cumsums, verifies shard/window metadata, then adds last-value persistence per-origin AE.
- Published Chronos-2 rolling paired-origin comparison and bootstrap/status artifacts at `experiment/artifacts/published/world_model_official_baselines_v2/20260425-chronos2-rolling-test-origin-errors-comparison.csv` and `20260425-chronos2-rolling-test-origin-errors-bootstrap-status.{json,csv}`.
- The comparison covers all `94458` rolling test origins. Prediction-count weighted proposed AE exactly matches the aggregate MAE `0.11734545799787531`; origin-level mean AE is persistence/Chronos `0.119063 / 0.117243`.
- Bootstrap direction is `baseline_abs_error_pu - proposed_abs_error_pu`, so positive values mean Chronos-2 has lower origin-level AE than last-value persistence. Rolling paired delta mean/95% CI = `0.001817 [0.001579, 0.002058]`, block delta = `0.001822 [0.001101, 0.002543]`, `prob_delta_gt_zero=1.0`.
- Claim boundary: Chronos-2 rolling bootstrap/status is now complete for the frozen zero-shot row, but Chronos is still zero-shot only rather than a trainable multiseed baseline. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 iTransformer trial24 multiseed follow-up
- Reran the validation-frozen iTransformer target-plus-exog residual trial24 config (`itransformer_target_plus_exog_d64_h4_e2_dropout0.1_lr0.0003_anchor1`) for seeds `3407,42,2026,17,926` with per-origin export at `experiment/artifacts/scratch/world_model_official_baselines_v2/itransformer_trial24_multiseed_origin_errors_20260425/`.
- Published combined seed rows and per-origin comparison at `experiment/artifacts/published/world_model_official_baselines_v2/20260425-itransformer-trial24-multiseed-origin-errors-seed-rows.csv` and `20260425-itransformer-trial24-multiseed-origin-errors-comparison.csv`; protocol-specific summaries/bootstrap live under `20260425-itransformer-trial24-multiseed-origin-errors-{rolling,nonoverlap}-*`.
- Five-seed test RMSE/MAE mean: rolling `0.265306 / 0.181279`, non_overlap `0.264912 / 0.181279`. This confirms the single-seed weak result rather than rescuing iTransformer with seed variance.
- Persistence-comparison bootstrap completed and is unfavorable to iTransformer. Rolling paired delta = `-0.062081 [-0.062425, -0.061735]`, block delta = `-0.062075 [-0.063625, -0.060513]`, `prob_delta_gt_zero=0.0`; non_overlap paired delta = `-0.063695 [-0.065712, -0.061719]`, block delta = `-0.063768 [-0.068982, -0.058867]`, `prob_delta_gt_zero=0.0`.
- Claim boundary: iTransformer trial24 now has five-seed mean/std and completed bootstrap status for the persistence comparison, but it is a weak official baseline, not a challenger. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-25 official baselines v2 paper-grade long-run implementation
- Implemented the code-side repair/driver layer for the planned paper-grade official-baseline rerun. Added `experiment/families/world_model_official_baselines_v2/diagnostics/long_run_driver.py`, which writes a resumable one-GPU queue under `experiment/artifacts/scratch/world_model_official_baselines_v2/long_run_20260425_paper_grade/`, snapshots git/GPU/artifact state, runs queued commands with per-item logs and exit JSON, selects only from validation CSVs, and materializes follow-on full-validation and frozen test/multiseed queues without reading test metrics for config selection.
- Added `dgcrn_official_core_residual_b3_geometry_v2` with B3/pairwise feature budget. The official DGCRN core now has a geometry-adjacency path using `A_ij = exp(-distance_in_rotor_diameters_ij / 4)` off diagonal, self-loop `1.0`, and `[A, A.T]` as predefined adjacency.
- Fixed TimeXer/iTransformer residual adapter inputs before any new rerun: residual variants center target-history channels by the last-value anchor and set official internal `use_norm=False`; direct variants keep absolute target-history inputs and `use_norm=True`. Result rows and per-origin rows now record `residual_input_mode` and `official_internal_norm`.
- Tightened origin-error export to fail on shape mismatch or zero-valid-origin coverage, instead of silently emitting unusable rows.
- Validation: `./.conda/bin/python -m pytest -q test/test_world_model_official_baselines_v2.py` passed (`26 passed, 1 skipped`). No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`, because this changed baseline infrastructure only and added no new mainline state-space module/output head.

2026-04-27 official baselines v2 paper-grade long-run results
- Completed the long-run queue through full validation, Gate B overfit64 repairs, and frozen test-once multiseed for the validation-selected non-DGCRN trainable baselines. Published the consolidated table and manifest under `experiment/artifacts/published/world_model_official_baselines_v2/20260425-paper-grade-*`; the run root is `experiment/artifacts/scratch/world_model_official_baselines_v2/long_run_20260425_paper_grade/`.
- Main-table completed rows now include controls/Chronos plus 5-seed iTransformer, TimeXer, and MTGNN rows. Full test coverage was verified for all trainable rows: rolling `94458` origins and non-overlap `2624` origins, with matching per-origin error rows and `selected_by=validation_only`, `no_test_feedback=true`, `uses_future_target=false`.
- Five-seed test RMSE/MAE means: iTransformer target-only residual B0 rolling `0.170498 / 0.117158`, non-overlap `0.169121 / 0.115918`; TimeXer target-only residual B0 rolling `0.169553 / 0.117016`, non-overlap `0.168526 / 0.116042`; MTGNN calendar residual B1 rolling `0.182942 / 0.119216`, non-overlap `0.180921 / 0.117690`.
- Controls/Chronos test rows in the final table: last-value rolling/non-overlap RMSE `0.186432 / 0.184142`; Ridge residual B0 `0.169658 / 0.168272`; Chronos-2 zero-shot B2 `0.183834 / 0.181359`.
- Persistence bootstrap is completed for iTransformer/TimeXer/MTGNN using per-origin 5-seed averaged AE comparisons. Positive delta means lower AE than last-value persistence. iTransformer rolling/non-overlap paired deltas `0.001994 / 0.001574`; TimeXer `0.002134 / 0.001450`; MTGNN `-0.000040 / -0.000186`, with MTGNN not distinguishable from persistence.
- DGCRN remains fail-closed for this paper-grade run: the validation-selected DGCRN B2 seed3407 frozen test-once ran for `24475.95s` and exited with native signal `-11` before producing test artifacts. It is excluded from `20260425-paper-grade-final-table.csv` and should be treated as appendix/diagnostic until a shard/recovery path exists.
- Validation after publication: `./.conda/bin/python -m pytest -q test/test_world_model_official_baselines_v2.py` passed (`27 passed`). No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`, because this closes baseline evidence only and adds no mainline state-space module/output head.

2026-04-30 DGCRN rescue shard run
- Implemented and completed the DGCRN recoverable checkpoint/shard rescue path for the validation-selected `dgcrn_official_core_residual_b2_v2` config (`hidden_dim=96`, `gcn_depth=3`, `dropout=0.0`, `lr=0.0005`, anchor `1`) under `experiment/artifacts/scratch/world_model_official_baselines_v2/long_run_20260425_paper_grade/dgcrn_rescue/`.
- All five seeds `3407,42,2026,17,926` now have full rolling and non-overlap test artifacts with origin-error exports, exact AE quantiles, and bootstrap status. Coverage is rolling `94458` origins / `20300940` predictions and non-overlap `2624` origins / `563951` predictions for each seed.
- Published rescue artifacts at `experiment/artifacts/published/world_model_official_baselines_v2/20260425-paper-grade-dgcrn-rescue-*`, including seed rows, mean/std summaries, protocol bootstrap/status files, per-origin comparisons, and `20260425-paper-grade-dgcrn-rescue-manifest.json`.
- Five-seed test RMSE/MAE means: rolling `0.171157 / 0.119189`, non-overlap `0.169605 / 0.117983`.
- Claim boundary: DGCRN remains `appendix_diagnostic`, not a main-table row, because seed42 required `recovered_from_checkpoint` and therefore fails the strict clean-training manifest rule. The rescue preserved validation-only selection (`selected_by=validation_only`, `no_test_feedback=true`, `uses_future_target=false`) and produced complete diagnostic evidence without using test metrics for config selection. No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`.

2026-04-30 official v2 + mainline validation leadwise recovery
- Added horizon-row export support to the official-baseline formal evaluator and DGCRN shard aggregate path so 36-step RMSE/MAE curves can be regenerated without choosing configs from test metrics. Overall metric rows remain public-table rows; per-lead arrays are expanded only when `--include-horizon-rows` is requested.
- Recovered DGCRN validation leadwise rows from the existing seed3407 checkpoint using shard validation for rolling and non-overlap under `experiment/artifacts/scratch/world_model_official_baselines_v2/leadwise_recovery_20260430/dgcrn_val_shards/`; checkpoint validation scale matched the prior selected result (`val` rolling RMSE about `0.1683`).
- Reran validation-only horizon exports for last-value, seasonal, Ridge residual B0, Chronos-2 non-overlap, iTransformer target-only residual B0, TimeXer target-only residual B0, and MTGNN calendar residual B1. Chronos-2 rolling and TFT-PF rolling leadwise rows were derived from existing validation shard aggregate JSONs.
- Updated `tools/make_leadwise_report.py` to produce combined official v2 + mainline 36-step validation plots and CSVs, including a no-seasonal version for readability. Main outputs: `figures/2026-04-30-official-v2-plus-mainline-validation-36step-leadwise-rmse.{csv,png,svg}` and `figures/2026-04-30-official-v2-plus-mainline-validation-36step-leadwise-rmse-no-seasonal.{png,svg}`.
- Validation: `./.conda/bin/python -m pytest -q test/test_world_model_official_baselines_v2.py` passed (`31 passed, 2 warnings`). No update to `plan/wind_farm_design_revised_v3_input_reorg.tex`, because this recovered baseline/report evidence only and added no new mainline state-space module/output head.
