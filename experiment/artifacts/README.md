# Experiment Artifacts

This directory contains runtime artifacts rather than experiment code.

- `published/`: canonical current formal results
- `runs/`: immutable per-invocation run manifests
- `scratch/`: disposable ad hoc outputs
